[theme]
# Tema escuro fixo: as cores dos gráficos foram validadas contra esta superfície.
base = "dark"
primaryColor = "#e5484d"
backgroundColor = "#0a0a0b"
secondaryBackgroundColor = "#141416"
textColor = "#f5f5f4"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
